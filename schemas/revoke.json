{
  "$id": "https://dedi.global/revoke.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Revoke or Negative List",
  "description": "Revocation list entry used to represent entities, credentials, keys, or memberships that are no longer valid in a defined scope.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "revoked_id": {
      "type": "string",
      "description": "Unique identifier of the subject being revoked."
    },
    "reason": {
      "type": "string",
      "description": "Reason for revocation or blacklisting."
    },
    "revokedAt": {
      "type": "string",
      "format": "date-time"
    },
    "revokedBy": {
      "type": "string",
      "description": "Identifier of the authority issuing the revocation."
    },
    "scope": {
      "type": "string",
      "description": "Scope within which the revocation applies."
    }
  },
  "required": [
    "revoked_id",
    "revokedAt",
    "revokedBy"
  ]
}