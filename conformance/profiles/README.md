# Conformance profiles

Profiles in this directory are machine-readable YAML definitions that bind named requirements to concrete tests or evidence assets.

- `base.yaml` defines the minimal interoperability target
- `secure.yaml` adds lifecycle and evidence expectations
