# Conformance

Conformance matters because a protocol without testable expectations turns interoperability into interpretation.

This directory defines a starter conformance surface for DeDi-compatible implementations.

## Included here

- a baseline compatibility profile,
- implementation-oriented test expectations,
- and a simple matrix for assessing minimum support.

This is not yet a full interop suite, but it is enough to move compatibility claims away from vague assertions.
