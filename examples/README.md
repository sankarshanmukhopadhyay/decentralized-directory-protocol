# Examples

Each schema directory includes:

- `sample.json`: a valid example payload
- `invalid.json`: an intentionally invalid payload showing a common failure mode

These examples are used by CI validation and should be kept aligned with schema changes.
